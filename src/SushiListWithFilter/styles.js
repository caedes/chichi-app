import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  form: {
    marginTop: "4rem",
  },
  search: {
    margin: "0 auto",
    width: 400,
    display: "flex",
  },
});
